/**
 * @deprecated Import from `@/data/typography-tokens` instead.
 * Thin re-export so older paths keep resolving.
 */
export {
  typeToken,
  typographyClassName,
  typeTokensBySize,
  TYPE_TOKENS,
  TYPE_DEMO_MIXED,
  TYPE_SIZES,
  TYPE_WEIGHTS,
  type TypeToken,
  type TypeTokenId,
  type TypeSize,
  type TypeWeight,
  type LegacyTypographyId,
} from "@/data/typography-tokens"
